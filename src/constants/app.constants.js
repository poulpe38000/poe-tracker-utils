const APP_CONSTANTS = {
    basename: process.env.PUBLIC_URL,
    drawerWidth: 250,
    routes: {
        root: '/',
        hideouts: {
            root: '/hideouts',
        },
        incursions: {
            root: '/incursions',
        },
        settings: {
            root: '/settings',
        },
    }
};

export default APP_CONSTANTS;